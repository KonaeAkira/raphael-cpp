#pragma once

#include <string>

enum struct Effect {
    InnerQuiet,
    WasteNot,
    Innovation,
    Veneration,
    GreatStrides,
    MuscleMemory,
    Manipulation,
    COUNT
};

enum struct Condition {
    Normal,
    Good,
    Excellent,
    Poor,
    Centered,
    Sturdy,
    Malleable,
    Primed,
    COUNT
};